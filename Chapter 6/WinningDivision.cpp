/*
		Winning Division
	Write a program that determines which of a company's four divisions 
	(Northeast, Southeast, Northwest, Southwest) had the greatest number of sales
	for a quarter. it should include the following functions.
		double getSales() is passed the name of a division. It asks the user for a division's 
		quartlery sales figure. validates the input, then returns it. it should be called 
		once for each division.
		void findHighest () is passed the four sales totals. It determines which is the largest 
		and prints the name of the high-grossing division along with its sales figure. 
	do not accept a dollar amount less than 0.00

*/